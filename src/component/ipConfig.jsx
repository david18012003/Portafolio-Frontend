export const IP = "192.168.0.105" 

/*const getChatBoxPosition = () => {
  const screenWidth = window.innerWidth;
  const screenHeight = window.innerHeight;

  // Tamaño de la burbuja y chat
  const chatBoxWidth = Math.min(500, screenWidth * 0.9);
  const chatBoxHeight = 400; // estimado (ajusta según tu chat)

  // Posición fija abajo a la derecha, con margen
  const right = 20;
  const bottom = 80; // espacio para el ícono

  return {
    position: 'fixed',
    right: `${right}px`,
    bottom: `${bottom}px`,
    width: `${chatBoxWidth}px`,
  };
};*/ 